import { createServerClient } from "@supabase/ssr";`nimport { cookies } from "next/headers";`n`nconst supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL;`nconst supabaseKey = process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY;`n`nexport const createClient = (cookieStore: Awaited<ReturnType<typeof cookies>>) => {`n  return createServerClient(`n    supabaseUrl!,`n    supabaseKey!,`n    {`n      cookies: {`n        getAll() {`n          return cookieStore.getAll()`n        },`n        setAll(cookiesToSet) {`n          try {`n            cookiesToSet.forEach(({ name, value, options }) => cookieStore.set(name, value, options))`n          } catch {`n            // The `setAll` method was called from a Server Component.`n            // This can be ignored if you have middleware refreshing`n            // user sessions.`n          }`n        },`n      },`n    },`n  );`n};
